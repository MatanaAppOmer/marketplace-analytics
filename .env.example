# Auth.js (NextAuth) — required for Google OAuth
# Generate AUTH_SECRET with: npx auth secret
AUTH_SECRET=

# Google OAuth 2.0 credentials (from Google Cloud Console)
AUTH_GOOGLE_ID=
AUTH_GOOGLE_SECRET=
